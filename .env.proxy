# Configuration Proxy pour EdgeTTS
# Remplacez par vos vraies valeurs de proxy si nécessaire
HTTP_PROXY=
HTTPS_PROXY=
NO_PROXY=localhost,127.0.0.1,speech.platform.bing.com
EDGE_TTS_PROXY=

# Configuration SSL/TLS
SSL_CERT_FILE=/etc/ssl/certs/ca-certificates.crt
REQUESTS_CA_BUNDLE=/etc/ssl/certs/ca-certificates.crt

# Configuration EdgeTTS spécifique
EDGE_TTS_VOICE=fr-FR-HenriNeural
EDGE_TTS_RATE=+0%
EDGE_TTS_VOLUME=+0%