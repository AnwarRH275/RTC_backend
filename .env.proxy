# Configuration Proxy pour EdgeTTS et services externes
# Laisser vide si aucun proxy n'est nécessaire

# Proxy HTTP/HTTPS (optionnel)
HTTP_PROXY=
HTTPS_PROXY=
NO_PROXY=localhost,127.0.0.1,mysql,speech.platform.bing.com

# Configuration EdgeTTS spécifique
EDGE_TTS_PROXY=
EDGE_TTS_VOICE=fr-CA-AntoineNeural
EDGE_TTS_RATE=+0%
EDGE_TTS_VOLUME=+0%

# Configuration SSL/TLS (optionnel)
SSL_CERT_FILE=
REQUESTS_CA_BUNDLE=